{
  "name": "zinger-pilot",
  "private": true,
  "version": "0.1.0",
  "description": "Zinger Pilot — user-facing app on usezinger.xyz (wallet, deposit, rules, signal execution)",
  "scripts": {
    "dev": "npx --yes serve public -l 4173",
    "deploy": "npx vercel --prod"
  }
}
